#!/usr/bin/env python
"""wercker-cli

Usage:
    wercker status
    wercker create
    wercker login
    wercker logout
    wercker target list
    wercker target add
    wercker target <name>
    wercker app link
    wercker app create
    wercker (-h | --help)
    wercker --version

Details:
    create - Adds an application to wercker *implemented*ma
    login  - Logs the user in and stores the token on the
             machine *implemented*
    forget - Removes the login token from the machine *implemented*
    """

import docopt

from werckercli.cli import (
    # get_parser,
    handle_commands,
    get_intro
)

from clint.textui import puts

puts(get_intro())
args = docopt.docopt(__doc__)
handle_commands(args)
